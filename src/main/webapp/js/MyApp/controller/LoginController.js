Ext.define('MyApp.controller.LoginController', {
    extend: 'Ext.app.Controller',

    views: [
        'MyApp.views.login.LoginView'
    ],
    init: function(application) { 
        this.control({ 
        });
    }
});